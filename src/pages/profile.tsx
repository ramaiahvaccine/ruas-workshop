import React from "react";

type Props = {};

const ProfilePage = (props: Props) => {
  return (
    <div>
      <p className="hero min-h-screen bg-base-200">Work in progress</p>
    </div>
  );
};

export default ProfilePage;
